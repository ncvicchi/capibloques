set(CAPI_REQUIRES nvs_flash json driver esp_timer esp_wifi esp_event esp_netif esp_hw_support mbedtls)
if(IDF_TARGET STREQUAL "esp32s3")
  list(APPEND CAPI_REQUIRES esp_driver_usb_serial_jtag esp_lcd)
endif()
idf_component_register(SRCS "main.cpp" INCLUDE_DIRS "." REQUIRES ${CAPI_REQUIRES})
if(NOT DEFINED CAPI_BOARD_ID)
  set(CAPI_BOARD_ID "wemos-d1-r32")
endif()
target_compile_definitions(${COMPONENT_LIB} PRIVATE CAPI_BOARD_ID="${CAPI_BOARD_ID}")
